NEBIUS_API_KEY=XXX

# MLflow tracking server (used by the DAG's summarize_and_log step).
# Local default works out of the box; override for docker-compose (e.g. http://mlflow:5000).
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=swebench-eval
